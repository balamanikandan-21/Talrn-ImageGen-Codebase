# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Env
venv/
env/
ENV/

# Streamlit
.streamlit/

# IDE
.vscode/
.idea/

# Project Specific
# We want to keep the sample outputs, so we won't ignore outputs/ globally
# But we might want to ignore future random outputs.
# For now, let's keep outputs/ tracked as per user request to commit samples.
